# Changelog

## 0.2.0
- Next-level mode: multi-repo mapping (`--map-repo` / config `map_repo`)
- Better spec links: markdown header anchor selection for GitHub blob links
- Improved search: ripgrep support (`--search-backend`)
- Test-gap hinting for HIGH severity rules
- Config file support (`--config specguard.yml`)
